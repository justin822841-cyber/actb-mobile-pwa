# ACTB Mobile PWA

Public mobile dashboard shell for ACTB.

This repository contains only static UI files and a derived `summary.json`.
It must not contain API keys, private `.env` files, or the full ACTB engine.
